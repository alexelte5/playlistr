[project]
name = "playlistr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alexelte5", email = "alexeltekow@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.2",
    "questionary>=2.1.1",
    "spotipy>=2.26.0",
]

[project.scripts]
playlistr = "playlistr:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
